from django.conf import settings
import pytest_common_subject

# def pytest_configure():
#     settings.configure(DATABASES=...)

# @pytest_common_subject.fixtures(autouse=True)
# def enable_db_access_for_all_tests(db):
#     pass